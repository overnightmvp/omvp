---
phase: 02-automation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agents/scraper.ts
  - src/lib/agents/types.ts
  - src/app/api/generation/scrape/route.ts
  - tests/lib/agents/scraper.test.ts
  - package.json
autonomous: true
requirements: [AUTO-01, AUTO-02]

must_haves:
  truths:
    - "System can fetch YouTube channel metadata via Apify"
    - "System can extract video transcripts for any YouTube video"
    - "Scraped data includes video title, description, view count, and transcript"
  artifacts:
    - path: "src/lib/agents/scraper.ts"
      provides: "Apify client wrapper for YouTube scraping"
      exports: ["scrapeYouTubeVideo", "ApifyClient"]
      min_lines: 80
    - path: "src/app/api/generation/scrape/route.ts"
      provides: "API endpoint to trigger video scraping"
      exports: ["POST"]
      min_lines: 40
    - path: "tests/lib/agents/scraper.test.ts"
      provides: "Test coverage for scraper with mocked Apify responses"
      min_lines: 60
  key_links:
    - from: "src/app/api/generation/scrape/route.ts"
      to: "src/lib/agents/scraper.ts"
      via: "import and call scrapeYouTubeVideo()"
      pattern: "import.*scrapeYouTubeVideo"
    - from: "src/lib/agents/scraper.ts"
      to: "apify-client"
      via: "ApifyClient initialization"
      pattern: "new ApifyClient"
---

<objective>
Integrate Apify SDK for YouTube video scraping and transcript extraction, providing structured data pipeline input.

Purpose: Enable automated content extraction from YouTube videos without hitting YouTube Data API quota limits. Apify provides unlimited scraping with anti-bot detection handling built-in.

Output: Production-ready scraper utility that fetches video metadata + transcripts via Apify actors, validated with tests.
</objective>

<execution_context>
@/Users/toshioj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toshioj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automation-pipeline/02-RESEARCH.md

# Phase 1 Foundation (relevant interfaces)
From Phase 1, we have:
- Supabase auth and session management
- `generation_queue` table with fields: id, user_id, video_url, status, priority, queued_at, error_message
- `youtube_connections` table with access tokens for API usage
- Environment variables pattern: NEXT_PUBLIC_*, process.env.* for server-side

# Research-Recommended Stack
Per 02-RESEARCH.md:
- Apify SDK (`apify-client`) for YouTube scraping
- YouTube Scraper actor (streamers/youtube-scraper) for metadata
- Subtitles included via `subtitlesLanguage: 'en'` parameter
- Expected cost: ~0.05-0.2 compute units (CU) per video
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Apify SDK and create scraper utility</name>
  <files>
    package.json
    src/lib/agents/scraper.ts
    src/lib/agents/types.ts
    tests/lib/agents/scraper.test.ts
  </files>
  <behavior>
    - Test 1: scrapeYouTubeVideo() with valid URL returns video metadata (title, description, viewCount)
    - Test 2: scrapeYouTubeVideo() with valid URL returns English transcript as plaintext
    - Test 3: scrapeYouTubeVideo() with invalid URL throws descriptive error
    - Test 4: scrapeYouTubeVideo() handles videos without transcripts gracefully (returns null transcript)
  </behavior>
  <action>
    1. Install apify-client: `npm install apify-client`
    2. Create src/lib/agents/types.ts with interfaces:
       - ScrapedVideoData { title, description, viewCount, transcript?, channelName, publishedDate }
       - ApifyScraperOptions { subtitlesLanguage, maxResults }
    3. Create src/lib/agents/scraper.ts:
       - Initialize ApifyClient with process.env.APIFY_API_TOKEN
       - Implement scrapeYouTubeVideo(videoUrl: string): Promise<ScrapedVideoData>
       - Use streamers/youtube-scraper actor with subtitlesLanguage: 'en', subtitlesFormat: 'text'
       - Parse Apify dataset response and map to ScrapedVideoData interface
       - Handle missing transcript scenario (some videos don't have captions)
    4. Create tests/lib/agents/scraper.test.ts:
       - Mock ApifyClient.actor().call() and ApifyClient.dataset().listItems()
       - Test all 4 behavior scenarios above
       - Use fixture data for Apify responses
    5. Add APIFY_API_TOKEN to .env.local.example with documentation
  </action>
  <verify>
    <automated>npm run test -- tests/lib/agents/scraper.test.ts -x</automated>
  </verify>
  <done>
    All tests pass. scrapeYouTubeVideo() returns validated ScrapedVideoData with title, description, viewCount, and transcript for valid YouTube URLs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create API route for video scraping</name>
  <files>
    src/app/api/generation/scrape/route.ts
  </files>
  <action>
    Create POST /api/generation/scrape endpoint:
    1. Validate request body with Zod: { queueItemId: string }
    2. Fetch queue item from generation_queue table (ensure status is 'pending' or 'processing')
    3. Extract video_url from queue item
    4. Call scrapeYouTubeVideo(video_url) from scraper.ts
    5. Update queue item with scraped data:
       - Store in new JSONB column: scraped_data (or create intermediate table if preferred)
       - Update status to 'scraped' on success
       - Update status to 'failed' + error_message on Apify errors
    6. Return JSON response: { success: true, data: ScrapedVideoData } or { success: false, error: string }
    7. Add error handling for:
       - Missing APIFY_API_TOKEN (return 500 with clear message)
       - Invalid video URL (return 400)
       - Apify rate limits (return 429 with retry-after header)
       - No transcript available (store null, don't fail — log warning)
  </action>
  <verify>
    Manual test via curl:
    ```bash
    # Create test queue item first
    curl -X POST http://localhost:3001/api/generation/scrape \
      -H "Content-Type: application/json" \
      -d '{"queueItemId": "test-queue-id"}'
    ```
    Response should return scraped video data with 200 status.
  </verify>
  <done>
    /api/generation/scrape endpoint successfully scrapes YouTube videos via Apify and updates generation_queue with scraped_data. Error scenarios handled with appropriate HTTP status codes.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Checks:**
1. Run `npm run test -- tests/lib/agents/scraper.test.ts` — all tests green
2. Check package.json has apify-client dependency
3. Verify .env.local.example documents APIFY_API_TOKEN
4. Test /api/generation/scrape with real Apify API token (use free trial if needed)
5. Confirm scraped data includes transcript field (or null if unavailable)
</verification>

<success_criteria>
- [ ] Apify SDK installed and integrated
- [ ] scrapeYouTubeVideo() function returns structured ScrapedVideoData
- [ ] Transcripts extracted in English plaintext format
- [ ] API endpoint /api/generation/scrape working with queue integration
- [ ] Tests cover happy path and error scenarios (invalid URL, missing transcript)
- [ ] No YouTube Data API quota usage (all scraping via Apify)
</success_criteria>

<output>
After completion, create `.planning/phases/02-automation-pipeline/02-01-SUMMARY.md`
</output>
