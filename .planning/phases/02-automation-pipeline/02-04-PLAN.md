---
phase: 02-automation-pipeline
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - middleware.ts
  - src/app/pages/[creator]/[slug]/page.tsx
  - src/lib/subdomain.ts
  - vercel.json
  - tests/lib/subdomain.test.ts
autonomous: true
requirements: [AUTO-08]

must_haves:
  truths:
    - "Pages are accessible via creator.platform.com/[slug] subdomain routing"
    - "Middleware rewrites subdomain requests to /pages/[creator]/[slug]"
    - "Published pages render with article content and schema markup"
  artifacts:
    - path: "middleware.ts"
      provides: "Subdomain routing logic"
      contains: "NextResponse.rewrite"
      min_lines: 40
    - path: "src/app/pages/[creator]/[slug]/page.tsx"
      provides: "Dynamic page renderer for generated content"
      min_lines: 80
    - path: "src/lib/subdomain.ts"
      provides: "Subdomain utilities (getUserFromSubdomain)"
      exports: ["getUserFromSubdomain", "isValidSubdomain"]
      min_lines: 30
    - path: "vercel.json"
      provides: "Wildcard domain configuration"
      contains: "domains"
  key_links:
    - from: "middleware.ts"
      to: "src/lib/subdomain.ts"
      via: "getUserFromSubdomain() to resolve creator"
      pattern: "getUserFromSubdomain"
    - from: "src/app/pages/[creator]/[slug]/page.tsx"
      to: "src/lib/db/generated-pages.ts"
      via: "getPage() to fetch published page"
      pattern: "getPage.*slug"
---

<objective>
Implement subdomain-based page publishing system with Next.js middleware routing.

Purpose: Enable multi-tenant architecture where each creator gets their own subdomain (creator.platform.com) for published SEO pages. Middleware handles routing without requiring DNS configuration for each creator.

Output: Working subdomain routing, dynamic page renderer, and Vercel wildcard domain configuration.
</objective>

<execution_context>
@/Users/toshioj/.claude/get-shit-done/workflows/execute-plan.md
@/Users/toshioj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automation-pipeline/02-RESEARCH.md

# Research-Recommended Pattern
Per 02-RESEARCH.md:
- Use Next.js middleware for subdomain detection and rewriting
- Pattern: hostname.split('.')[0] to extract subdomain
- Rewrite creator.platform.com/article → /pages/[creator]/[slug]
- Skip rewriting for main domain (platform.com) and localhost
- Environment variable for domain configuration: NEXT_PUBLIC_ROOT_DOMAIN

# Existing Middleware
Current middleware.ts handles Supabase session refresh. Need to extend, not replace.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create subdomain utilities with validation</name>
  <files>
    src/lib/subdomain.ts
    tests/lib/subdomain.test.ts
  </files>
  <behavior>
    - Test 1: getUserFromSubdomain() returns creator handle from valid subdomain
    - Test 2: getUserFromSubdomain() returns null for main domain (platform.com)
    - Test 3: isValidSubdomain() rejects reserved subdomains (www, api, app, admin, blog)
    - Test 4: extractSubdomain() handles localhost correctly (returns null)
    - Test 5: extractSubdomain() handles production domains with multiple dots
  </behavior>
  <action>
    Create src/lib/subdomain.ts:
    1. Define RESERVED_SUBDOMAINS: ['www', 'api', 'app', 'admin', 'blog', 'dashboard', 'auth']
    2. Implement extractSubdomain(hostname: string): string | null
       - Parse hostname, remove root domain (from env var NEXT_PUBLIC_ROOT_DOMAIN)
       - Handle localhost: return null
       - Handle production: return subdomain or null if main domain
    3. Implement isValidSubdomain(subdomain: string): boolean
       - Check not in RESERVED_SUBDOMAINS
       - Check matches pattern: /^[a-z0-9-]{3,30}$/
    4. Implement getUserFromSubdomain(hostname: string): Promise<string | null>
       - Extract subdomain using extractSubdomain()
       - Validate using isValidSubdomain()
       - Query profiles table for user with matching handle/username
       - Return user_id or null if not found
    5. Create tests for all scenarios
  </action>
  <verify>
    <automated>npm run test -- tests/lib/subdomain.test.ts -x</automated>
  </verify>
  <done>
    Subdomain utilities validate and resolve creator identities from hostnames. Tests pass for localhost and production scenarios.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend middleware for subdomain routing</name>
  <files>
    middleware.ts
  </files>
  <action>
    Update existing middleware.ts to add subdomain routing BEFORE Supabase session refresh:
    1. Import extractSubdomain() from src/lib/subdomain.ts
    2. Early in middleware function, detect subdomain:
       ```typescript
       const hostname = request.headers.get('host') || ''
       const subdomain = extractSubdomain(hostname)
       ```
    3. If subdomain exists and isValidSubdomain(subdomain):
       - Extract pathname from request.nextUrl.pathname
       - Rewrite to /pages/[creator]/[slug] route:
         ```typescript
         const url = request.nextUrl.clone()
         url.pathname = `/pages/${subdomain}${pathname}`
         return NextResponse.rewrite(url)
         ```
    4. If no subdomain or main domain, continue to existing Supabase middleware logic
    5. Update matcher config to exclude subdomain routes from Supabase refresh:
       ```typescript
       export const config = {
         matcher: [
           '/((?!_next|_static|favicon.ico|pages).*)',
         ],
       }
       ```
    6. Add environment variable check: if (!process.env.NEXT_PUBLIC_ROOT_DOMAIN) skip subdomain logic (for local dev)
  </action>
  <verify>
    Manual test:
    1. Add test subdomain to /etc/hosts: `127.0.0.1 testcreator.localhost`
    2. Visit http://testcreator.localhost:3001/test-slug
    3. Check Network tab for rewritten path: /pages/testcreator/test-slug
  </verify>
  <done>
    Middleware rewrites subdomain requests to /pages/[creator]/[slug] route. Existing Supabase session logic preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dynamic page renderer for published content</name>
  <files>
    src/app/pages/[creator]/[slug]/page.tsx
  </files>
  <action>
    Create src/app/pages/[creator]/[slug]/page.tsx:
    1. Define params interface: { creator: string, slug: string }
    2. Fetch page data in async server component:
       - Query generated_pages table by creator handle + slug
       - Use getPage() utility from src/lib/db/generated-pages.ts
       - Return 404 if page not found or not published (published_at is null)
    3. Render page content:
       - Inject schema markup in <head> via next/head or inline script tag:
         ```typescript
         <script type="application/ld+json" dangerouslySetInnerHTML={{
           __html: JSON.stringify(page.schema_markup)
         }} />
         ```
       - Render article headline as <h1>
       - Render meta description in <meta name="description"> tag
       - Render content as markdown using react-markdown or @tailwindcss/typography
       - Render FAQ section separately with structured markup
    4. Add metadata export for SEO:
       ```typescript
       export async function generateMetadata({ params }): Promise<Metadata> {
         const page = await getPage(params.creator, params.slug)
         return {
           title: page.headline,
           description: page.meta_description,
         }
       }
       ```
    5. Style with Tailwind using dark theme from Phase 1
    6. Add "Powered by Authority Platform" footer with link back to main site
  </action>
  <verify>
    Manual test:
    1. Create test page in generated_pages table
    2. Visit subdomain URL: http://testcreator.localhost:3001/test-article
    3. Verify headline, content, and FAQ render correctly
    4. Inspect <head> for JSON-LD script tag with schema markup
    5. Check meta description tag present
  </verify>
  <done>
    Dynamic page route renders published content with JSON-LD schema markup in <head>. Metadata export provides SEO tags.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Subdomain routing system with middleware rewriting and dynamic page rendering. Pages accessible at creator.platform.com/[slug] with JSON-LD schema markup embedded.
  </what-built>
  <how-to-verify>
    1. Create test page via /api/generation/schema endpoint
    2. Update page's published_at timestamp to NOW() in Supabase
    3. Add test subdomain to /etc/hosts (for local testing): `127.0.0.1 testcreator.localhost`
    4. Visit http://testcreator.localhost:3001/[slug] where [slug] is generated slug
    5. Verify page loads with:
       - Headline as <h1>
       - Content rendered as formatted markdown
       - FAQ section visible
       - JSON-LD script tags in <head> (inspect with DevTools)
       - Meta description in <head>
    6. Test 404 handling: visit non-existent slug, should show 404
    7. Test reserved subdomain rejection: http://www.localhost:3001/anything should NOT rewrite
  </how-to-verify>
  <resume-signal>Type "approved" if subdomain routing works correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
**Overall Phase Checks:**
1. Run `npm run test -- tests/lib/subdomain.test.ts -x`
2. Verify middleware.ts preserves existing Supabase session logic
3. Test subdomain extraction with various hostnames
4. Confirm /pages/[creator]/[slug] route renders published pages
5. Validate JSON-LD schema markup appears in rendered HTML
</verification>

<success_criteria>
- [ ] Middleware detects subdomains and rewrites to /pages/[creator]/[slug]
- [ ] Reserved subdomains (www, api, admin) blocked from rewriting
- [ ] Dynamic page route renders article content with schema markup
- [ ] Published pages accessible via creator.platform.com/[slug] (in production)
- [ ] Local development supports subdomain testing with /etc/hosts
- [ ] 404 handling for non-existent pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-automation-pipeline/02-04-SUMMARY.md`
</output>
